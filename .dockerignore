# First exclude only specific directories/files that should be ignored
.git/
.vscode/
.idea/
__pycache__/
*.py[cod]
*$py.class
*.so
# Exclude all environment files for security
.env
*.env
*env*/
.streamlit/
.DS_Store
Thumbs.db
node_modules/
# Make sure we don't exclude the entire frontend directory
# Only exclude the node_modules inside frontend
frontend/node_modules/

# Explicitly include frontend package.json and lock file
!frontend/package.json
!frontend/package-lock.json

# By default, everything else is included
